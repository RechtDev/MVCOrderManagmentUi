@*@model SearchView

@{
    ViewData["Title"] = " | Search Results";
}

<div class="row">
    <div class="col">
        <h1 class="mb-2">@Model.Header</h1>
    </div>
    <div class="col">
        <div class="float-right pr-2">
            <a asp-action="Index">New Search</a>
        </div>
    </div>
</div>

<table class="table">
    <tbody>
        @foreach (var Product in Model.Product)
        {
            <tr>
                <td>
                    <partial name="_ProductLinkPartial" model="@Product" />
                </td>
                <td>@Product.ProdName</td>
                <td>
                    @foreach (Models ba in Product.Product)
                    {
                        <div><partial name="_ProdPriceLinkPartial" model="@ba.ProdPrice" /></div>
                    }
                </td>
                <td>
                    <div class="float-right">
                        <a asp-action="Edit" asp-route-id="@Product.ProdId"
                           my-button>Edit</a>
                        <a asp-action="Delete" asp-route-id="@Product.ProdId"
                           my-button>Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
s*@